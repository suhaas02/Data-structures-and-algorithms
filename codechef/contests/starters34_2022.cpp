#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while (t--)
    {
        int n,x;
        cin>>n>>x;
        int high = 1000;
        int a[n];
        for (int i = 0; i < n; i++)
        {
            cin>>a[i];
        }
        int sum = n * x;
        if (sum > high)
        {
            sum = 
        }
        
    }
    
}